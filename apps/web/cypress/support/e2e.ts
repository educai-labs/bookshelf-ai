// Comandos y config de Cypress E2E (feature 013).
// Los tests de dashboard dependen de la sesión; se mockea la API
// (`/api/v1/*`) con `cy.intercept` en cada spec para no depender del backend.

export {};
