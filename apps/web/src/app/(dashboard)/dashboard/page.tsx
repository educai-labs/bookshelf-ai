/**
 * Placeholder del dashboard — la UI real (grid de libros) se implementa
 * en la feature 013.
 */
export default function DashboardPage() {
  return <h1 className="text-2xl font-semibold">Dashboard - Feature 013</h1>;
}
